import { PageStub } from "@/components/shared/PageStub";

export default function Home() {
  return (
    <PageStub
      title="Agrisoil"
      note="Home page content (hero, stats, product teasers) is scheduled for M2, pending brand voice and stat confirmation."
    />
  );
}
